[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --strict-markers
markers =
    unit: Unit tests (no database, no external calls)
    integration: Integration tests (require database)
    slow: Tests that take > 5 seconds
